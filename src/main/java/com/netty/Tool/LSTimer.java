package com.netty.Tool;

import java.util.TimerTask;

public class LSTimer extends TimerTask {
   int count = 0;


    @Override
    public void run() {


      // for (WebSocketServerHandlerEX handler : App.clientMap.values()){

      //     if(handler.context!= null){

      //        // LMessager.sendMessage("前端，我给你消息",handler.context);
      //        // System.out.println(handler.ID);
      //     }

      // }
      // System.out.println( App.clientMap.size());
      // System.out.println("发送一次！");
    }
}
